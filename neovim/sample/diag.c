
#include <stdio.h>

/*
 * Prints an array `a` with length `n`.
 */
void printarr (int * a, int n)
{
  for (int i = 0; i < n; i++)
  {
    printf("%i ", a[i]);
  }
  char c = 2345;
}

int main (int argc, char ** argv)
{
  char s [20] = "hello world";
  printf ("%s\n", s);
  return;
}

